# 🚀 Getting Started with ChaseMyCareer

## Welcome!

This guide will help you set up and run ChaseMyCareer locally in just a few minutes.

## 📋 What You Need

Before starting, make sure you have:

- ✅ **Node.js 18+** installed ([Download here](https://nodejs.org/))
- ✅ **npm** (comes with Node.js)
- ✅ **Supabase account** ([Sign up free](https://supabase.com))
- ⚠️ **Docker Desktop** (optional, for local Supabase)

## 🎯 Quick Start (5 Minutes)

### Option 1: Interactive Setup (Recommended)

```bash
# Run the interactive setup wizard
./setup.sh
```

The wizard will guide you through:
1. Checking prerequisites
2. Restructuring the project
3. Installing dependencies
4. Configuring environment variables
5. Starting development servers

**That's it!** Just follow the prompts.

### Option 2: Manual Setup

```bash
# 1. Restructure the project
./restructure.sh

# 2. Install all dependencies
npm run install:all

# 3. Configure environment
cp frontend/.env.example frontend/.env.local
cp backend/.env.example backend/.env

# 4. Edit environment files with your Supabase credentials
nano frontend/.env.local
nano backend/.env

# 5. Start development
npm run dev
```

## 🔑 Getting Supabase Credentials

### Step 1: Create Supabase Project

1. Go to [supabase.com/dashboard](https://supabase.com/dashboard)
2. Click "New Project"
3. Fill in project details
4. Wait for project to be created (~2 minutes)

### Step 2: Get Your Credentials

1. In your project dashboard, go to **Settings** → **API**
2. Copy these values:

   - **Project URL** → Use for `VITE_SUPABASE_URL`
   - **anon public key** → Use for `VITE_SUPABASE_ANON_KEY`

3. Add them to `frontend/.env.local`:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

### Step 3: Apply Database Migrations

```bash
cd backend
npx supabase link --project-ref your-project-ref
npx supabase db push
```

## 🏃 Running the Application

### Run Both Frontend and Backend

```bash
npm run dev
```

This starts:
- **Frontend:** http://localhost:5173
- **Backend:** http://localhost:54323 (if using local Supabase)

### Run Separately

**Terminal 1 - Frontend:**
```bash
cd frontend
npm run dev
```

**Terminal 2 - Backend (optional, for local Supabase):**
```bash
cd backend
npm run start
```

## ✅ Verify Everything Works

After starting, check:

1. **Frontend loads:** Visit http://localhost:5173
2. **No console errors:** Open DevTools (F12) → Console
3. **Can access login page:** Click "Sign In"
4. **Database connected:** Try logging in with test account

### Test Accounts

After applying migrations, you can use:

- **Email:** demo@chasemycareer.de
- **Password:** DemoUser2024!

## 🐛 Common Issues & Solutions

### Issue: "Command not found: ./setup.sh"

**Solution:**
```bash
chmod +x setup.sh restructure.sh
./setup.sh
```

### Issue: "Port 5173 already in use"

**Solution:**
```bash
lsof -ti:5173 | xargs kill -9
npm run dev
```

### Issue: "Module not found" errors

**Solution:**
```bash
npm run clean
npm run install:all
```

### Issue: "Supabase connection failed"

**Solution:**
1. Check `frontend/.env.local` exists and has correct values
2. Verify Supabase URL and key are correct
3. Restart dev server: `Ctrl+C` then `npm run dev`

### Issue: Environment variables not working

**Solution:**
- Ensure file is named `.env.local` (not `.env`)
- File must be in `frontend/` directory
- Restart dev server after changes
- Variables must start with `VITE_`

## 📚 Next Steps

### Learn the Project

1. **Read the main README:**
   ```bash
   cat README_NEW.md
   ```

2. **Explore frontend:**
   ```bash
   cat frontend/README.md
   ```

3. **Explore backend:**
   ```bash
   cat backend/README.md
   ```

### Start Developing

1. **Frontend development:**
   - Edit files in `frontend/src/`
   - Changes auto-reload in browser
   - Components in `frontend/src/components/`
   - Pages in `frontend/src/pages/`

2. **Backend development:**
   - Database migrations in `backend/supabase/migrations/`
   - Edge functions in `backend/supabase/functions/`
   - Create migration: `cd backend && npm run migration:new my_feature`

### Useful Commands

```bash
# Frontend
cd frontend
npm run dev          # Start dev server
npm run build        # Build for production
npm run lint         # Check code quality

# Backend
cd backend
npm run start        # Start local Supabase
npm run db:push      # Apply migrations
npm run db:reset     # Reset database

# Both
npm run dev          # Run both (from root)
npm run build        # Build frontend
npm run lint         # Lint frontend
```

## 🎓 Learning Resources

### Project Documentation

- **[LOCAL_DEVELOPMENT_GUIDE.md](./LOCAL_DEVELOPMENT_GUIDE.md)** - Complete setup guide
- **[frontend/README.md](./frontend/README.md)** - Frontend documentation
- **[backend/README.md](./backend/README.md)** - Backend documentation
- **[PROJECT_RESTRUCTURING_GUIDE.md](./PROJECT_RESTRUCTURING_GUIDE.md)** - Migration guide

### External Resources

- [React Documentation](https://react.dev) - Learn React
- [Supabase Documentation](https://supabase.com/docs) - Learn Supabase
- [Tailwind CSS](https://tailwindcss.com) - Learn Tailwind
- [shadcn/ui](https://ui.shadcn.com) - Component library

## 🆘 Need Help?

### Quick Help

1. **Run setup wizard:** `./setup.sh`
2. **Check documentation:** See files listed above
3. **Check troubleshooting:** Each README has a troubleshooting section

### Detailed Help

1. **For setup issues:** Check [LOCAL_DEVELOPMENT_GUIDE.md](./LOCAL_DEVELOPMENT_GUIDE.md)
2. **For frontend issues:** Check [frontend/README.md](./frontend/README.md)
3. **For backend issues:** Check [backend/README.md](./backend/README.md)
4. **For migration:** Check [PROJECT_RESTRUCTURING_GUIDE.md](./PROJECT_RESTRUCTURING_GUIDE.md)

### Still Stuck?

1. Check browser console for errors (F12 → Console)
2. Check terminal output for error messages
3. Verify environment variables are correct
4. Try clean install: `npm run clean && npm run install:all`

## 🎉 You're Ready!

Once everything is running, you can:

- ✅ Access the application at http://localhost:5173
- ✅ Log in with test account
- ✅ Explore the 50-day job search program
- ✅ Track job applications
- ✅ Practice interviews
- ✅ Manage networking contacts

## 📊 Project Structure Quick Reference

```
chasemycareer/
├── frontend/          # React app - edit here for UI changes
│   ├── src/          # Source code
│   │   ├── components/  # React components
│   │   ├── pages/       # Page components
│   │   ├── contexts/    # React contexts
│   │   └── services/    # API services
│   └── package.json  # Frontend dependencies
│
├── backend/          # Supabase - edit here for database changes
│   ├── supabase/
│   │   ├── migrations/  # Database schema
│   │   └── functions/   # Edge functions
│   └── package.json  # Backend dependencies
│
└── package.json      # Root - run both together
```

## 🚀 Quick Commands Cheat Sheet

```bash
# Setup
./setup.sh                    # Interactive setup wizard
./restructure.sh              # Restructure project
npm run install:all           # Install all dependencies

# Development
npm run dev                   # Run both frontend + backend
cd frontend && npm run dev    # Run frontend only
cd backend && npm run start   # Run backend only

# Building
cd frontend && npm run build  # Build for production

# Database
cd backend
npm run db:push              # Apply migrations
npm run db:reset             # Reset database
npm run migration:new name   # Create migration

# Cleaning
npm run clean                # Remove node_modules
npm run clean:install        # Clean and reinstall
```

## 💡 Pro Tips

1. **Use the setup wizard** - It's the easiest way to get started
2. **Keep terminal open** - Watch for errors and warnings
3. **Check browser console** - Many errors show up there (F12)
4. **Read the READMEs** - They have detailed information
5. **Use test accounts** - Don't create real accounts during development

## 🎯 What's Next?

After getting the app running:

1. **Explore the features** - Try all the tools
2. **Read the code** - Understand how it works
3. **Make changes** - Start with small UI tweaks
4. **Add features** - Build something new
5. **Deploy** - Share your work with others

---

**Happy coding!** 🚀

**Questions?** Check the documentation files or run `./setup.sh` for guided setup.
