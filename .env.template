# Environment Variables Template

# ============================================================================
# Supabase Configuration (Existing - Keep these)
# ============================================================================
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ============================================================================
# Cloudflare Configuration (New - Add these)
# ============================================================================

# Cloudflare Account ID
# Find at: Cloudflare Dashboard → Account → Account ID
VITE_CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id

# Cloudflare API Token
# Create at: Cloudflare Dashboard → My Profile → API Tokens
# Required permissions: Account.Analytics, Workers KV Storage, Workers Scripts, Zone.Cache Purge
VITE_CLOUDFLARE_API_TOKEN=your_cloudflare_api_token

# Cloudflare Zone ID (for your domain)
# Find at: Cloudflare Dashboard → Select Domain → Overview → Zone ID
VITE_CLOUDFLARE_ZONE_ID=your_cloudflare_zone_id

# Cloudflare Worker URL
# After deploying worker, this will be: https://your-worker-name.your-subdomain.workers.dev
VITE_CLOUDFLARE_WORKER_URL=https://your-worker.workers.dev

# Cloudflare R2 Public URL (optional, if using R2)
# After setting up R2 bucket with public access
VITE_CLOUDFLARE_R2_PUBLIC_URL=https://your-bucket.r2.dev

# ============================================================================
# Google OAuth (Existing - Keep these)
# ============================================================================
VITE_GOOGLE_CLIENT_ID=your_google_client_id

# ============================================================================
# Application Configuration (Existing - Keep these)
# ============================================================================
VITE_APP_ID=app-80an9ymym5mp
VITE_API_ENV=production
